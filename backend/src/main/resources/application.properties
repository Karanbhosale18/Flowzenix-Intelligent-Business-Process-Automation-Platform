# ==============================
# Server
# ==============================
server.port=8080

# ==============================
# PostgreSQL Datasource
# ==============================
spring.datasource.url=jdbc:postgresql://localhost:5432/FlowZenix_DB
spring.datasource.username=postgres
spring.datasource.password=Karan@dev
spring.datasource.driver-class-name=org.postgresql.Driver

# ==============================
# JPA / Hibernate
# ==============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# ==============================
# JWT
# ==============================
# 256-bit+ secret, replace in production and load from an env var / secrets manager instead
app.jwt.secret=${JWT_SECRET:KnjbdhcbHGEY#*^!$@uuiUHFGER@&y8uwHU@&T^EGYEYFGE^TQWji8ye7gwydg7ew}
# 24 hours in ms
app.jwt.expirationMs=${JWT_EXPIRATION_MS:840000}

# ==============================
# CORS (frontend dev server)
# ==============================
app.cors.allowedOrigins=${CORS_ORIGINS:http://localhost:5173}


# ==============================
# Swagger
# ==============================
springdoc.swagger-ui.path=/doc

