-- Run once against Postgres to create the database used by application.properties.
-- (Tables themselves are auto-created by Hibernate via spring.jpa.hibernate.ddl-auto=update)

CREATE DATABASE FlowZenix_DB;
